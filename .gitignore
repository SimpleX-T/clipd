/target/
**/*.rs.bk
Cargo.lock.bak
*.swp
*.swo
.DS_Store

# Cargo vendoring artefacts — populated by scripts/build-ppa.sh before
# uploading to Launchpad, but never committed to git. dpkg-source picks
# them up from the working tree at source-package time.
/vendor/
/.cargo/config.toml
/.cargo-home/

# Local debian build artefacts
/debian/clipd/
/debian/.debhelper/
/debian/debhelper-build-stamp
/debian/files
/debian/*.substvars
/debian/*.log
*.deb
*.dsc
*.changes
*.buildinfo
*.upload
*.tar.xz
*.tar.gz

# Editor / IDE
.vscode/
.idea/
